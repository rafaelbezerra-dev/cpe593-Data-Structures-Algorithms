import java.io.FileNotFoundException;
import java.io.FileReader;
import java.util.Scanner;

public class Boggle {
	
	class BoggleBoard {
		
	}
	
	public static boolean canMoveToPos(char[][] board, boolean[][] board, int pos) {
		if (pos >= 0 )
		return true;
	}

	public static void main(String[] args) throws FileNotFoundException {
		// TODO Auto-generated method stub
		Trie dict = new Trie();
		Scanner in = new Scanner(new FileReader("dictionary.txt"));
		while (in.hasNextLine()) {
			String line = in.nextLine();
			dict.add(line);
		}
		in.close();

		in = new Scanner(new FileReader("boggle.dat"));
		while (in.hasNextLine()) {
			String line = in.nextLine();
			System.out.println(line + "\t" + dict.contains(line));
		}
		in.close();

	}

}
